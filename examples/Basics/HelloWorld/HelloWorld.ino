#include <Makey32.h>

// the setup routine runs once when Makey32 starts up
void setup(){

  // Initialize the Makey32 object
  Makey.begin();

  // LCD display
  Makey.Lcd.print("Hello World");
}

// the loop routine runs over and over again forever
void loop() {

}
